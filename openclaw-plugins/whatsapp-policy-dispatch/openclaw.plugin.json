{
  "id": "whatsapp-policy-dispatch",
  "name": "WhatsApp Policy Dispatch",
  "description": "Routes WhatsApp inbound messages through the local whatsapp-chatbot policy worker before OpenClaw starts a native agent run.",
  "version": "0.1.0",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean"
      },
      "endpoint": {
        "type": "string"
      },
      "timeoutMs": {
        "type": "number"
      }
    }
  }
}
